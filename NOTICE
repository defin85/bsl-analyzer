BSL Analyzer — licensing notice / лицензионная заметка

The workspace is dual-licensed per crate. See LICENSING.md for the full
per-crate matrix and docs/legal/ for the underlying provenance audit.

  * Tier A crates are licensed under MIT OR Apache-2.0.
  * Tier B crates are licensed under LGPL-3.0-or-later.
  * The shipped LSP server binary, which statically links Tier B crates,
    is therefore distributed under LGPL-3.0-or-later. Source for all
    crates is published in this repository.

Architecture heritage
---------------------
The overall architecture — event-based parser, Rowan CST, Salsa
incremental database, HIR and IDE layering — follows the design of
rust-analyzer, which is distributed under MIT OR Apache-2.0.
Upstream: https://github.com/rust-analyzer/rust-analyzer

Language heritage
-----------------
BSL and the SDBL query language are part of the 1C:Enterprise platform.
The language itself, its keywords, its syntax and its semantic rules
belong to 1C as the platform author; they are not proprietary to any
third-party reimplementation.

Official references:
  * 1C query language: https://its.1c.ru/db/pubqlang
  * 1C code standards: https://its.1c.ru/db/v8std

Diagnostic rules
----------------
Code-quality diagnostics in crates/ide-diagnostics encode the 1C
code-style standards published at v8std.ru (CC0) and in the official
1C documentation. They do not implement a third party's proprietary
rule set.

Derivation notice for the SDBL and BSL grammar layers
-----------------------------------------------------
The initial grammar work in the following files was written with the
grammar files of the bsl-parser project
(https://github.com/1c-syntax/bsl-parser, LGPL-3.0-or-later) open as
the working reference:

  * crates/lexer/src/sdbl/        (SDBL token inventory)
  * crates/parser/src/grammar/sdbl/  (SDBL grammar)
  * crates/parser/src/grammar/    (BSL grammar layer)
  * crates/lexer/src/lib.rs       (BSL token inventory)
  * crates/syntax/src/preproc_symbols.rs
                                  (preprocessor symbol list; the file it
                                  succeeds,
                                  crates/ide-diagnostics/src/utils/preprocessor_symbols.rs,
                                  carried a header naming the
                                  PREPROC_*_SYMBOL tokens of BSLLexer.g4 as
                                  the source)

The last path lies outside the parser and lexer crates and was missing
from this list until 2026-08-21. It is kept here because the omission was
in the record, not in the licensing. The list itself moved to
crates/syntax on 2026-09-04, when the four diverging copies of it were
merged into one registry; the material and its 4.8.1.2 attestation moved
with it, out of the Tier B crate it used to live in.

Test material is a provenance class of its own. The commit that
introduced the diagnostic for unknown preprocessor symbols also recorded
"Test fixture copied verbatim from bsl-language-server". Deleting the
fixture file later moved that material into an inline test rather than
retiring it. It has now been replaced with material derived from section
4.8.1.2 of the 1C Developer's Guide, and a test in the crate fails if the
retired material reappears.

A sweep of every BSL test input in the tree, done on 2026-08-21, found
that the unknown-preprocessor-symbol fixture was one case of a class.
The commits that introduced diagnostic fixtures say in their own words
that test files were copied from bsl-language-server and from
bsl-language-server-rust. 07d2b977 deleted 187 such fixtures when the
tests moved inline, and the material of a large part of them is still
present in crates/ide-diagnostics.

What is stated here is that class, not a per-file map. Four independent
ways of building such a map were tried, and each failed in its own
direction: attributing a fixture to the commit that merely moved it,
crediting shared boilerplate as surviving material, matching a line any
test could contain, and following a rename into a sibling fixture. A
textual match also cannot see material a test builds programmatically.
A record that looks precise and is repeatedly wrong — sometimes
understating what was taken, sometimes attributing our own work to
others — is worse than a narrower record that holds. The remedy for the
uncertainty is not a better map but replacement of the material, tracked
in https://github.com/itrous/bsl-analyzer/issues/52.

Two things are listed file by file, because both are established without
heuristics: the 21 fixtures added by 4601ffcb, which that commit itself
enumerates, and crates/parser/tests/fixtures/Module.bsl. Both are in the
table in LICENSING.md. The evidence in full is in
docs/legal/bsl-clean-room-slice-b4.md.

These facts are stated here directly, and not left to be discovered in
the commit history, because the public mirror of this repository does
not carry that history: a disclosure that can only be verified in a
private repository is not a disclosure. The record below is given so
that a reader who has only this file can rely on it.

This is recorded in this repository's own history of December 2025,
though not uniformly per file. The commit that introduced the SDBL lexer
names both upstream grammar files in its commit message and in the file
header. The commit that introduced the SDBL grammar names SDBLParser.g4
in the file header and quotes upstream grammar rules in ANTLR notation in
many of its function doc-comments. For the BSL layer the record is the
planning document shipped in the initial commit, which names the upstream
grammars as the source for both languages; a file header naming
BSLLexer.g4 was added a few commits later. Details and commit identifiers
are in docs/legal/sdbl-provenance-2026-07-audit.md.

The SDBL layer has since been rewritten in bounded slices from official
1C documentation under the clean-room process described in
docs/legal/sdbl-clean-room-slices.md, and now diverges from the upstream
grammar in token inventory, token granularity, rule decomposition and
operator precedence. That rewrite is not finished.

The BSL layer is being rewritten the same way, under
docs/legal/bsl-clean-room-slices.md. All four sub-slices are complete: the
BSL token inventory (bsl-clean-room-slice-b1.md) and the preprocessor
symbol list with its test material (b2.md), both re-derived from Chapter
4 of the 1C Developer's Guide for 8.3.27; the grammar rules, attested
rule by rule rather than rewritten (b3.md); and the test material, whose
origin was established where the history establishes it and left stated
as unestablished where it does not (b4.md).

The compatibility question that ran alongside them is recorded as a
decision in docs/legal/bsl-compatibility-decision.md. Compatibility with
bsl-language-server on BSL parsing stays a stated goal of this product;
what the record establishes is that no parsing rule was adopted on the
strength of that implementation. Both commits that changed what the
parser accepts derive their rule from Chapter 4 by section, and the
other implementation supplied a corpus-scale cross-check of a decision
already taken. The record names its own limit: it reads commit
messages, so a silent comparison would not appear in it.

What this does and does not deliver. The BSL half of exit-criteria item
10 is now complete: the token inventory was re-derived, the preprocessor
symbols were re-derived, every grammar rule carries a verdict with the
section it rests on, the test material's origin is recorded, and the
compatibility decision is written down. It does not by itself relicense
anything. Three items of the checklist remain open — the SDBL parser
rule naming, the comparison of sdbl-hir against the upstream lowering,
and the test corpus — and until they close, the crates named above stay
under LGPL-3.0-or-later. What has changed is that for crates/parser and
crates/lexer the only thing standing between them and Tier A is the
remaining SDBL work, which is a position neither crate has been in
before.

The current assessment — what is proven, what has diverged, and what is
still outstanding — is in docs/legal/sdbl-provenance-2026-07-audit.md.
Until that work completes, the Tier B crates and the final binary remain
under LGPL-3.0-or-later.

Notice for crates written with a copyleft reference open
---------------------------------------------------------
The notice above covers the grammar layers only. Three crates that are
currently licensed MIT OR Apache-2.0 were also written with a
copyleft-licensed project open as the working reference. The three are
not equivalent: the first two inherited structure, the third matched
observable behaviour on purpose and copied nothing. In each case
the annotations saying so were removed from the source in March and
April 2026 without the code being rewritten, so the source as it stands
today carries no trace of this. It is therefore stated here.

  * crates/cfg — the control-flow-graph model was written with the CFG
    package of bsl-language-server (LGPL-3.0-or-later) open, by way of
    an earlier Rust project by the same author. The file headers read
    "Ported from BSL Language Server (Java)" and carried per-method
    "Maps to ... in Java" annotations. The builder architecture is
    independent: a recursive descent over HIR, where the reference uses
    a block writer over a parse tree, and the two share no structural
    device. What follows the reference is the vertex and edge taxonomy,
    its granularity, the decomposition of loops, try/except and labels,
    and the part of the graph API that is not the underlying graph
    library's own vocabulary.

  * crates/bsl-metadata — the crate described itself as "a Rust port of
    the Java mdclasses library" (https://github.com/1c-syntax/mdclasses,
    LGPL-3.0) when it was created, with per-method "Java equivalent"
    annotations. The parsing implementation is independent, and most of
    the model is dictated by the 1C configuration export format itself,
    which is the format this crate has to read. What is inherited is the
    ontology: the set of named types, the split into traits, the builder
    pattern, and part of the method naming.

  * crates/hir-def — several diagnostic ranges and metrics were written
    to reproduce the observable output of bsl-language-server and of the
    ANTLR-generated parser it uses. Those commits changed comments only;
    no code was copied. The behaviour was kept deliberately, so that the
    two tools report the same numbers on the same source.

The licensing tier of crates/cfg and crates/bsl-metadata is under review
in light of the above.

Compatibility with bsl-language-server
--------------------------------------
Where this project reports the same diagnostics as bsl-language-server,
the agreement is deliberate. Rule codes, tags, severities, default
activation and the places a rule fires form an interface that existing
SonarQube quality profiles and CI pipelines depend on; a tool that
reported different rules under different names on the same code would
not replace its predecessor. These values match that project's published
rule documentation, which is where they can be checked.

This is a statement about observable behaviour, not about source code:
implementing the same interface independently is intended, and copying
implementation text is not.

Bundled 1C platform data
------------------------
The file crates/bsl-platform/data/platform_data.json is a structured
extract of the 1C:Enterprise context-help archives (shcntx_ru.hbk,
shlang_ru.hbk). The contents — in particular method descriptions,
parameter documentation and code examples — are © ООО «1С-Софт» and
are NOT covered by this workspace's MIT, Apache-2.0 or LGPL-3.0
licensing. The file is bundled to allow reproducible builds of
bsl-analyzer without requiring every contributor to install
1C:Enterprise locally, and on the practical assumption that its use is
interoperability with the 1C platform rather than a standalone
documentation product. See crates/bsl-platform/data/PROVENANCE.md for
the full statement.

Upstream acknowledgements
-------------------------
  * rust-analyzer — architectural reference — MIT OR Apache-2.0
  * bsl-parser — grammar reference used when the SDBL and BSL layers
    were first written — LGPL-3.0-or-later
  * bsl-language-server — deliberate compatibility target for the
    configuration format, the diagnostic catalogue and BSL parsing;
    also the reference used when crates/cfg and parts of crates/hir-def
    were first written — LGPL-3.0-or-later. Where parsing is concerned
    the decision is recorded in docs/legal/bsl-compatibility-decision.md:
    both rules that changed what the parser accepts come out of Chapter 4
    by section, and that project supplied a corpus-scale cross-check
    rather than the rule.
  * mdclasses — object-model reference used when crates/bsl-metadata
    was first written — LGPL-3.0
  * v8std.ru — public catalogue of 1C coding standards — CC0
  * crates/parser/tests/fixtures/Module.bsl — © ООО «1С-Софт»,
    CC BY 4.0, redistributed with its own license header intact. The
    copy was taken from the test resources of bsl-parser, which had
    itself adapted it as a fixture; the content and its license are
    1C's, not that project's. CC BY 4.0 asks that modifications be
    indicated: the file carries the previous adaptation in its own
    header, and this repository changed two further lines of the body
    in f1fc00ff (2026-05-13), correcting an event-handler statement to
    the syntax section 4.6.11.1 gives. The license header itself has
    never been touched, and a test fails if it is removed or altered.
  * crates/bsl-metadata/fixtures/designer/{Catalogs,CommonModules,
    Documents,Ext,HTTPServices,InformationRegisters,WebServices} —
    21 BSL files introduced by 4601ffcb, whose message records
    "Comprehensive test fixtures copied from bsl-language-server-rust".
  * crates/bsl-platform/data/platform_data.json — © ООО «1С-Софт»,
    1C copyright, bundled for interoperability; see
    crates/bsl-platform/data/PROVENANCE.md

See LICENSING.md for the per-crate license map and the conditions
under which each crate can be reused.
